/**
 * @param {string[]} strs
 * @return {string}
 */
var longestCommonPrefix = function(strs) {
    var startItem = strs[0];
    for (var i = 0, len1 = startItem.length; i < len1; i++){
        for (var j = 0, len2 = strs.length; j < len2; j++) {
            if(startItem.substring(i, )){

            }
        }
    }
};

longestCommonPrefix(["flower","flow","flight"])
